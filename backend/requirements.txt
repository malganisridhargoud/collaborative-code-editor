# -----------------------------
# Core Django
# -----------------------------
Django==4.2.7
djangorestframework==3.14.0
django-cors-headers==4.3.0
python-dotenv==1.0.0
djangorestframework-simplejwt

# -----------------------------
# ASGI + WebSockets
# -----------------------------
channels==4.0.0
daphne==4.0.0
channels-redis==4.1.0
python-dotenv

# -----------------------------
# Redis client
# -----------------------------
redis>=4.5.0

# -----------------------------
# HTTP & utilities
# -----------------------------
requests==2.31.0
gunicorn
psycopg2-binary


# -----------------------------
# Google OAuth (Backend)
# -----------------------------
google-auth==2.29.0
google-auth-oauthlib==1.2.0
google-auth-httplib2==0.2.0

# -----------------------------
# Security / crypto (needed by auth & channels)
# -----------------------------
cryptography>=41.0.0
PyJWT>=2.8.0

# -----------------------------
# Production server helpers
# -----------------------------
asgiref>=3.6.0
